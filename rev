#include "main.h"
/**
* print_rev - print a string reverse
* @cad: string.
* Return: 0 if success.
*/

int print_rev(int cad)
{
	int conta = 0;
	char *str;

	while (str[conta + 1])
	{
		cad++;
	}

	for (conta = cad - 1; conta >= 0; conta--)
	{
		_putchar(str[conta]);
	}
	return (cad);

}
