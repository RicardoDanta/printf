#include "main.h"
/**
* print_rev - print a string reverse
* @cad: argument to print.
* Return: 0 if success.


int print_rev(int cad)
{
	int conta = 0;
	char *s;

	while (s[conta + 1])
	{
		cad++;
	}

	for (conta = cad - 1; conta >= 0; conta--)
	{
		_putchar(s[conta]);
	}
	return (cad);

}
*/
